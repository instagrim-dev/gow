# G4 open authoring-to-materializer delivery v8

This record closes the bounded open-only delivery slice. It does not authorize
or report protected authoring, a protected retry, G4 evaluation, spending, or
G5 advancement.

The preserved V7 public reproducer passed JSON decoding and its advertised
schema, then failed at Go expression admission before route compatibility or
staging. The request used Ollama `/api/chat` with a JSON Schema in `format`;
this is not evidence that OpenAI strict Structured Outputs was active. The
public reproducer identifies a V7 interface mismatch without assigning that
root cause to the protected unit 00 response.

V8 makes the model select typed endpoint ASTs and one of seven frozen recipe
identifiers. The host derives starting expressions, rule sequences, bindings,
premises, costs, history records, intermediate states, renderings, and traces.
It rejects invalid choices without selecting another route. This constrains
open authoring diversity to the frozen recipe catalog and does not attribute
host construction to model capability.

At source revision `5cc77a96ed548d5838825f37f7f2b6b97ed74953`, the deterministic
acceptance run completed all 24 open units at the frozen
`24/2048/45/49152/1080` vector. It exercised the real request adapter, command
line materializer, staging, assembly, executable-backed identity calculation,
and return validation. Malformed provider JSON, malformed and undeclared
expressions, unsupported recipes, timeouts, and interruption after five staged
units were refused without a completed assembly or return.

The separately frozen live open run used local model `qwen2.5:7b-instruct`
with digest `845dbda0ea48ed749caafd9e6037047aa19acfcfd82e704d7ca97d631a0b697e`.
All three predeclared calls completed on their first and only attempt. The run
staged three units, assembled the complete population, calculated artifact
identities with the pinned executable, and validated the final return in
22.761 seconds. The exact result and content-free identities are in
`completion.json`.

The retained replay evidence lives at
`.newf/reviews/2026-09-16-g4-open-authoring-repair/original`. Raw live responses
are public synthetic material but remain outside the maintained repository.
