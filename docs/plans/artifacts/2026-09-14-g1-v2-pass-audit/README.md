# G1 v2 pass audit

This directory publishes the content-free records for the completed `g1-v2-20260914T013604Z-semantic` dispatch. The protected task, answer, provenance, and receipt files remain outside the repository.

The [freeze audit](G1_V2_FREEZE_AUDIT.json) binds the dispatched custodian brief, public interface, executable, authorization reference, approved acceptance-matrix revision, and pre-execution seal. The [custodian return packet](G1_V2_CUSTODIAN_RETURN_PACKET.json) and independently produced [grade packet](G1_V2_GRADE_PACKET.json) are copied byte-for-byte from the sealed local return paths and have companion SHA-256 files.

The separate grade records `pass` at `agent-sealed/v1`. That grade is bounded by the shared-host procedural custody boundary and is not independent-human or training-lineage evidence. It establishes this frozen v2 scope; it does not close the broader G1 capability claim.
